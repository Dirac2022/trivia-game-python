name: Python CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3  # Usar la versión 3 para mayor compatibilidad
      - name: Set up Python 3.9
        uses: actions/setup-python@v3  # Usar la versión 3
        with:
          python-version: '3.9'
      - name: Set up Java 17
        uses: actions/setup-java@v3  # Usar la versión 3
        with:
          java-version: '17'
          distribution: 'temurin'  # Usar Temurin como distribución de Java
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run pytest
        run: pytest
      - name: Verify Java Version
        run: java -version
      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@v1
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: https://sonarcloud.io
        with:
          args: >
            -Dsonar.projectKey=trivia-game-python
            -Dsonar.sources=.